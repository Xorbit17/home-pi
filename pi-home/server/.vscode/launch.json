{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Django: run job WEATHER",
      "type": "debugpy",
      "request": "launch",
      "program": "${workspaceFolder}/manage.py",
      "args": [
        "job",
        "WEATHER"
      ],
      // "args": ["job_tick"],
      "django": true,
      "cwd": "${workspaceFolder}",
      "console": "integratedTerminal"
    }
  ]
}
